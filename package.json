{
  "name": "ai-hub-claude_ai",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test",
    "test:browser": "node scripts/browser-smoke.mjs",
    "backup:restore": "node scripts/restore-backup.mjs"
  }
}
